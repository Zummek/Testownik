import { ActionTree } from 'vuex';
import { State } from '../index';
import { QuizSessionsState } from './types';

const actions: ActionTree<QuizSessionsState, State> = {
  // Declare your actions here
};

export default actions;
